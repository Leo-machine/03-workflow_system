==============================================================
流程办事地图 · M1 整栈验收记录（OrbStack）
日期：2026-07-27 00:30（北京时间）
结论：M1 全部 13 项任务完成；PG 测试组 5/5、整栈验收 8/8 通过
==============================================================

【一】环境落地
- OrbStack 2.2.1 安装于 /Applications（brew cask，经本机 7897 代理加速，
  直连 orbstack.dev 不可达，代理后 ~13MB/s）
- Docker 引擎：server 29.4.0 / overlayfs / OrbStack VM
- Docker Hub 直连不可达（Bad Gateway），已配 registry mirror：
  ~/.orbstack/config/docker.json →
  docker.m.daocloud.io、docker.nju.edu.cn、docker.1ms.run
- 本会话内使用方式：
    export DOCKER_HOST=unix://$HOME/.orbstack/run/docker.sock
    /Applications/OrbStack.app/Contents/MacOS/xbin/docker-compose ...
  备注：docker CLI 符号链接（/usr/local/bin/docker）需打开一次
  OrbStack GUI 完成首次向导后才会建立。

【二】PG 集成测试（pytest -m pg，testcontainers + postgres:16-alpine）
5/5 通过（26s）：
- Alembic 迁移全新 PG 执行 + downgrade/upgrade 往返
- 迁移结果与 SQLAlchemy 模型 metadata 零 diff
- assignments.role_id 主键冲突 → IntegrityError
- step_roles (step_id, role_id) 联合主键冲突 → IntegrityError
- seed 幂等：第二次整体跳过，行数不变

【三】检查点1：compose 后端联调（自动 migrate + seed）
backend 容器日志：
  [entrypoint] 1/3 alembic upgrade head
  INFO  Running upgrade -> 0001, init: 流程办事地图 M1 全部 9 张表
  [entrypoint] 2/3 python -m app.seed（幂等）
  [seed] 种子数据已灌入：1 条流程 / 10 环节 / 9 岗位 / 9 人 / 2 账号
  [entrypoint] 3/3 uvicorn 0.0.0.0:8000 → Application startup complete.

【四】检查点2：整栈验收（docker compose up --build，3 容器，经 nginx :8080）
0) 前端页面 HTTP 200
1) admin 经 nginx 登录 OK
2) 种子流程可见：物理机服务器资源申请，10 环节，010 责任人 练宇婷
3) 改派 owner→贺法 → {"changed":true,"change_log_id":1}
4) 刷新后 010 责任人 → 贺法（改一处处处同步，PG 持久化）
5) 变更记录 → old_name=练宇婷 new_name=贺法 changed_by=admin（姓名快照）
6) viewer 改派 → HTTP 403（只读无管理入口）
7) 同人 no-op → {"changed":false,"change_log_id":null}
验收后已把 owner 恢复为练宇婷（新增 change_log id=2，正好演示留痕）。

【五】访问入口（容器运行中）
- 应用：http://localhost:8080 （admin/admin123 管理，viewer/viewer123 只读）
- API 文档：http://localhost:8000/docs

常用运维命令：
  cd /Users/leo/Workflow_Visualization_system
  docker compose ps / logs -f backend / down / up -d
（若 docker 不在 PATH，先打开一次 OrbStack.app 完成首次向导）

【六】遗留事项（M1 之外，已在代码留 TODO）
- 接内网 4A/SSO；token 改 httpOnly cookie 或 SSO 机制
- 迁内网前改 .env 默认密码与 JWT_SECRET
- 内网构建建议：提前备 wheelhouse / npm 离线包或内网镜像源
==============================================================
